{
  // Root tsconfig — for IDE navigation only. Do not use for compilation.
  // Type-checking is done per-environment:
  //   npm run typecheck:renderer  → tsconfig.renderer.json (DOM + ESNext)
  //   npm run typecheck:electron  → tsconfig.electron.json (Node + CommonJS)
  //   npm run typecheck           → both, in sequence
  "references": [
    { "path": "./tsconfig.renderer.json" },
    { "path": "./tsconfig.electron.json" }
  ],
  "files": []
}
